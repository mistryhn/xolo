import { useState } from 'react'; import { socket } from '../../lib/socket'; import { useChatStore } from '../../store/chatStore';
export function ChatWindow() { const { activeChatId, messages, add } = useChatStore(); const [body, setBody] = useState(''); if (!activeChatId) return <p>Select a chat to start talking.</p>; return <section><div>{messages.map((m) => <article key={m.id}><b>{m.alias}</b>: {m.body} <button onClick={() => socket.emit('chat:reaction', { messageId: m.id, emoji: '❤️' }, () => {})}>❤️</button></article>)}</div><form onSubmit={(e) => { e.preventDefault(); const clientId = crypto.randomUUID(); const text = body.trim(); if (!text) return; add({ id: clientId, chatId: activeChatId, body: text, alias: 'You', senderId: 'me', createdAt: new Date().toISOString(), clientId }); socket.emit('chat:message', { chatId: activeChatId, body: text, clientId }, () => {}); setBody(''); }}><input value={body} maxLength={1000} onChange={(e) => setBody(e.target.value)} placeholder="Say something"/><button>Send</button></form></section>; }
