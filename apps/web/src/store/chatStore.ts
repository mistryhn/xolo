import { create } from 'zustand'; export type Message = { id: string; chatId: string; body: string; alias: string; senderId: string; createdAt: string; clientId: string }; export const useChatStore = create<{ activeChatId: string | null; messages: Message[]; setActive: (id: string) => void; add: (m: Message) => void }>((set) => ({ activeChatId: null, messages: [], setActive: (activeChatId) => set({ activeChatId, messages: [] }), add: (m) => set((s) => s.messages.some((x) => x.id === m.id || x.clientId === m.clientId) ? s : { messages: [...s.messages, m] }) }));
